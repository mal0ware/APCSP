# fill in the `citation` function to return the author's name in the correct format
def citation(author_name):
    cited = str(author_name[2]) + ", " + str(author_name[0]) + " " + str(author_name[1])
    return cited